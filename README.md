Decoder C# practice project that handles the Pyramid algorithm. 
Decode message using the last number in each level of the pyramid
Handles an input file with numbers, uses given decoder to write message. 


